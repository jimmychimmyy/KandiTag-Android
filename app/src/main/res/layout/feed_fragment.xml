<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent"
    tools:context="com.jimchen.kanditag.FeedFragment">



    <!--

    <TextView android:text="KandiTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="25dp"
        android:layout_marginTop="10dp"
        android:textColor="@color/white"
        android:id="@+id/kandiTagTextView"/>

        -->

    <ImageView
        android:layout_alignParentBottom="true"
        android:id="@+id/FeedFragment_bottomBar"
        android:layout_width="fill_parent"
        android:layout_height="75dp"
        android:background="@color/black"
        android:src="@drawable/kanditag_splashscreenlogo"
        android:paddingLeft="220dp"
        android:paddingRight="10dp"/>

    <com.jimchen.kanditag.BounceListView
        android:layout_above="@+id/FeedFragment_bottomBar"
        android:id="@+id/FeedFragment_ListView"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:background="@color/transparent"
        android:stretchMode="columnWidth"
        android:minHeight="80dp"
        android:backgroundTint="@color/transparent"
        android:listSelector="@android:color/transparent"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:scrollbars="none"/>

    <!--

    <ListView
        android:id="@+id/FeedFragment_ListView"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:background="@color/transparent"
        android:stretchMode="columnWidth"
        android:minHeight="80dp"
        android:backgroundTint="@color/transparent"
        android:listSelector="@android:color/transparent"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"
        android:scrollbars="none"/>

        -->



</RelativeLayout>
